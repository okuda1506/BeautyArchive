# Beauty Archive - Codex Development Workflow

## 1. 目的

本ドキュメントは、Beauty Archive の開発を Codex / AI
エージェント主体で進めるための開発ルールを定義する。

Codex は、原則として以下の開発サイクルを自律的に完結させること。

``` text
Issue
  ↓
Branch
  ↓
Implementation
  ↓
Self Review
  ↓
Build / 必要な検証
  ↓
Pull Request
  ↓
CI
  ↓
Merge to main
  ↓
Close Issue
  ↓
Delete Branch
```

ユーザーに毎回実装確認・マージ確認を求めるのではなく、本ドキュメントに定義された完了条件を満たしている場合は、Codex
自身で次の工程へ進めること。

------------------------------------------------------------------------

## 2. 基本方針

-   1 Issue = 1目的を原則とする。
-   Issue 単位で作業ブランチを作成する。
-   実装・修正・テスト・PR作成・main
    へのマージ・Issueクローズまで、原則として Codex 自身が行う。
-   Issue に記載された範囲を超える大規模な変更は行わない。
-   実装後は必ず自己レビューを行う。
-   実施したテストがコードの不具合で失敗した場合は、原因を特定して解消すること。
-   検証の種類は変更内容とPCへの負荷に応じて選び、未実施項目はPRと完了報告に明記する。
-   main ブランチを常に動作可能な状態に保つ。
-   変更履歴を追跡できるよう、Pull Request を必ず経由する。
-   CI が設定されている場合は、CI 成功後にのみ main へマージする。

------------------------------------------------------------------------

## Codex 使用量が少ない場合の停止ルール

このルールは重要事項として、他の開発フローより優先する。

Codex の残り使用量が
**10%を切った場合は、その時点で実装作業を停止すること。**

### 停止時のルール

-   残り使用量が 10%
    未満になったことを確認したら、それ以上の実装を進めない
-   新しい修正・リファクタリング・テスト追加などを開始しない
-   作業途中であっても停止してよい
-   停止のためのコミットは不要
-   PR作成・mainへのマージ・Issueクローズも行わない
-   使用量を節約するために、無理に作業を完了させようとしない
-   未コミットの変更が残っていても、そのまま停止してよい
-   現在どこまで作業したかを簡潔にユーザーへ報告する

停止後は、Codex 側の判断で自動的に実装を再開してはならない。

使用量が復活した後、ユーザーから明示的に

``` text
続けて
```

と指示された場合にのみ、停止した箇所から実装を再開すること。

### 再開時

再開時は、まず以下を確認する。

-   現在のブランチ
-   `git status`
-   未コミットの変更内容
-   直前まで実装していた Issue
-   未完了の作業
-   未実行または未完了のテスト

確認後、停止前の作業内容を引き継いで実装を継続する。

この停止ルールは、通常の「実装 → テスト → PR →
mainマージ」の完了ルールより優先する。

## 3. Issue 作成

新しい機能追加・修正・リファクタリングを開始する際は、Codex 自身で
GitHub Issue を作成すること。

Issue には最低限、以下を記載する。

### 必須項目

-   目的
-   背景
-   実装内容
-   対象範囲
-   対象外
-   完了条件（Acceptance Criteria）
-   必要に応じて関連画面・関連モデル・関連API

### Issue例

``` md
## 目的

Hair の美容記録を新規登録できるようにする。

## 実装内容

- Hair記録作成画面を追加
- 日付、サロン、メニュー、金額、メモを入力可能にする
- 写真をPhotosPickerから登録可能にする
- SwiftDataへ保存する

## 対象外

- Google Calendar連携
- CloudKit同期エラーUI
- HairStyleReferenceとの紐付け

## 完了条件

- 新規記録を保存できる
- 保存後にArchiveへ反映される
- 必須項目のバリデーションが動作する
- Buildが成功する
- 変更内容に応じた検証結果と未実施項目が明記されている
```

------------------------------------------------------------------------

## 4. ブランチ運用

Issue を作成したら、main から作業ブランチを作成すること。

### ブランチ命名例

``` text
feature/42-add-hair-record
fix/51-calendar-event-duplication
refactor/63-beauty-record-model
chore/70-update-project-settings
```

原則として以下のプレフィックスを使用する。

  種別               プレフィックス
  ------------------ ----------------
  新機能             `feature/`
  バグ修正           `fix/`
  リファクタリング   `refactor/`
  設定・環境・雑務   `chore/`
  テストのみ         `test/`
  ドキュメント       `docs/`

Issue番号をブランチ名に含めることを推奨する。

------------------------------------------------------------------------

## 5. 実装

実装は Codex 自身が行う。

### 実装ルール

-   Issue の完了条件を満たすために必要な変更のみを行う。
-   無関係なリファクタリングを同じ Issue に混在させない。
-   既存アーキテクチャ・命名規則・設計方針を尊重する。
-   SwiftUI の標準API・Apple純正コンポーネントを優先する。
-   対応OSで利用できる場合は、Apple純正の Liquid Glass / SwiftUI API
    を優先し、独自の疑似Glass実装を乱用しない。
-   UI変更時は Light / Dark Mode を考慮する。
-   アクセシビリティを損なう実装を避ける。
-   一時的なデバッグコード、不要な
    `print`、仮データを本番コードに残さない。
-   秘密情報・APIキー・トークンをソースコードへハードコードしない。
-   既存機能を壊す可能性がある場合は必要な回帰テストを検討する。シミュレータでの実行は7章の負荷方針に従う。

------------------------------------------------------------------------

## 6. Self Review

実装完了後、検証を行う前に Codex
自身で変更内容をレビューすること。

最低限、以下を確認する。

-   `git diff` を確認したか
-   Issue に関係ない変更が含まれていないか
-   不要なファイルが追加されていないか
-   デバッグコードが残っていないか
-   命名が不自然でないか
-   重複コードが増えていないか
-   既存設計と矛盾していないか
-   Force unwrap など不要な危険実装がないか
-   Optional / Error 処理が適切か
-   UI状態の扱いに漏れがないか
-   Issue の Acceptance Criteria を満たしているか

問題があれば、この段階で修正する。

------------------------------------------------------------------------

## 7. Build / Test

PCのCPU負荷を抑えるため、ローカルのiOS Simulatorを使うUnit Test・UI Testを**毎回自動実行しない**。シミュレータの起動や並列テストは、開発中の通常の検証手段にしない。

### 通常の検証

-   アプリコードを変更した場合は、まず差分の自己レビューと`git diff --check`を行い、シミュレータを起動しない`xcodebuild build`（generic iOS Simulator destination）でコンパイルを確認する。
-   ドキュメントや開発ルールだけの変更では、内容と差分を確認する。Xcode Build・Unit Test・UI Testは不要とする。
-   変更に関係する軽量な静的検証があれば実施する。
-   実行した検証、実行しなかったテスト、その理由をPRと完了報告に記載する。未実施を成功と表現しない。
-   新規のコンパイルエラーや重大なWarningを増やさず、IssueのAcceptance Criteriaを満たす。

### シミュレータを使う検証

-   ローカルのUnit Test・UI Test・画面確認は通常の作業で自動実行しない。ユーザーが明示的に依頼した場合、またはリリース前などの節目に必要性とCPU負荷を伝えて実行する。
-   実行時は対象のテストとシミュレータを絞り、並列実行を避ける。負荷が高い、または動作が重くなった場合は停止する。
-   UI変更時のLight / Dark Mode、Dynamic Type、主要画面サイズ、Liquid Glassの可読性は、可能な範囲でSwiftUI Previewや軽量な方法で確認する。実機・シミュレータで未確認の項目は明記する。
-   シミュレータでのテスト未実施だけを理由に、PR作成やmainへのマージを止める必要はない。ただし実施したテストがコードの不具合で失敗している状態では完了扱いにしない。

### SwiftData・CloudKit・Google Calendar関連変更時

保存・同期・認可・APIの実動作確認が必要な変更では、影響範囲と未確認のリスクをPRに記載し、適切な検証方法を選ぶ。負荷の高いシミュレータテストを自動で開始しない。

-   SwiftData：保存・読み込み・更新・削除、既存データ、マイグレーションへの影響。
-   CloudKit：SwiftDataとの整合性、iCloud利用不可・同期失敗時の挙動、Schemaへの影響。
-   Google Calendar：未連携時の基本機能、認可・APIエラー、イベント重複、イベントIDの対応。

実動作を確認していない項目は確認済みと表現せず、必要な節目で検証する。

------------------------------------------------------------------------

## 8. テスト失敗時のルール

Build Errorや、実施したテストで確認できたコードの不具合は原因を調査して修正し、該当する検証を再実行する。テストを通すためだけに既存テストを削除・無効化してはならない。既存テストを変更する場合は理由をPRに記載する。

シミュレータの高負荷・タイムアウト・環境上の問題でテストを中断した場合は、無理に繰り返さない。コードの失敗と環境要因を区別し、未実施または中断した範囲を報告する。

------------------------------------------------------------------------

## 9. Pull Request

7章の通常の検証とSelf Reviewを終えたら、Codex 自身で main 向け Pull Request
を作成すること。

### PRに記載する内容

-   概要
-   対応Issue（作成した場合）
-   主な変更内容
-   実行した検証と結果
-   未実施・中断したテストと理由
-   UI変更がある場合は確認内容
-   既知の制約
-   追加対応が必要な事項があればその内容

### PR例

``` md
## 概要

Hair美容記録の新規登録機能を追加しました。

Closes #42

## 変更内容

- HairRecordCreateViewを追加
- PhotosPicker対応
- BeautyRecordのSwiftData保存処理を追加
- 入力バリデーションを追加

## テスト

- Xcode Build: Passed
- Unit Tests: 18 Passed
- UI Tests: 3 Passed
- Simulator: iPhone Pro / Light・Dark Mode確認済み

## 備考

Google Calendar連携は本Issueの対象外です。
```

------------------------------------------------------------------------

## 10. CI

CI が設定されている場合、Pull Request の CI
がすべて成功していることを確認する。

以下の状態では main へマージしてはならない。

-   CI実行中
-   CI失敗
-   必須チェック未完了
-   コンフリクトあり

CI が失敗した場合は原因を特定し、作業ブランチ上で修正する。

------------------------------------------------------------------------

## 11. mainへのマージ

以下をすべて満たした場合、Codex はユーザーへの追加確認なしで main
にマージしてよい。

-   Issueを作成した場合はAcceptance Criteriaを満たしている
-   Self Review 済み
-   アプリコード変更時はBuild成功、文書のみの変更時は差分確認済み
-   7章で必要な検証を実施し、未実施項目と理由を記録済み
-   実施したテストにコード起因の失敗がない
-   PR 作成済み
-   CI がある場合はすべて成功
-   コンフリクトなし
-   「ユーザー確認必須の変更」に該当しない

原則として Pull Request を経由して main へマージすること。

main への直接 push は行わない。

------------------------------------------------------------------------

## 12. ユーザー確認が必須の変更

以下の変更は、テストが成功していても Codex の判断だけで main
へマージしてはならない。

変更内容と影響範囲をユーザーへ説明し、確認を得ること。

### 必須確認対象

-   SwiftDataモデルの破壊的変更
-   保存済みユーザーデータを失う可能性がある変更
-   CloudKit Schema の破壊的変更
-   CloudKit Container設定変更
-   Bundle Identifier変更
-   App ID変更
-   Signing / Team / Provisioning変更
-   Capabilitiesの重大な変更
-   iCloud設定の重大な変更
-   Google OAuth Client設定の変更
-   APIキー・認証情報・Secretsに関係する変更
-   Package / 外部依存ライブラリの大規模な追加・置換
-   大量のファイル削除
-   Git履歴を書き換える操作
-   main ブランチへの force push
-   データマイグレーションを伴う重大変更
-   App Store公開設定に直接影響する変更
-   課金・サブスクリプション関連の変更

上記以外でも、不可逆性が高い変更や影響範囲が不明確な場合は確認を優先する。

------------------------------------------------------------------------

## 13. Issueクローズ

main へのマージ完了後、対応する Issue をクローズすること。

PR の本文に以下の形式を利用できる場合は利用する。

``` text
Closes #42
```

マージ後に Issue が自動クローズされていることを確認する。

自動クローズされていない場合は Codex 自身でクローズする。

------------------------------------------------------------------------

## 14. ブランチ削除

main へのマージ完了後、不要になった作業ブランチを削除する。

リモート・ローカル双方について、今後不要であることを確認して整理する。

------------------------------------------------------------------------

## 15. 完了報告

すべて完了したら、ユーザーへの報告は簡潔に行う。

最低限以下を含める。

``` text
Issue #42 完了

- Hair記録作成機能を実装
- Build成功
- Unit Test / UI Test: 未実施（シミュレータ負荷を避けるため）
- PR #43 を main にマージ済み
- Issue #42 クローズ済み
```

ユーザーが詳細を求めていない場合、実装途中の細かいログを大量に報告する必要はない。

ただし、重要な設計判断・仕様変更・制約がある場合は明記する。

------------------------------------------------------------------------

## 16. 禁止事項

Codex は以下を行わないこと。

-   テスト失敗状態で完了扱いにする
-   Build Error を残したまま PR を作成する
-   CI失敗状態で main にマージする
-   Issueと無関係な大規模変更を同時に行う
-   mainへ直接pushする
-   ユーザー確認必須の変更を勝手にマージする
-   テストを通すためだけに既存テストを無効化する
-   エラーを握りつぶす
-   秘密情報をソースコードへコミットする
-   `.env`、APIキー、OAuth Secret等を誤ってコミットする
-   理由なく依存ライブラリを増やす
-   UIの確認内容と未確認のリスクを伏せたまま完了扱いにする
-   既存ユーザーデータを破壊する可能性のある変更を無断で実行する
-   force push等でGit履歴を不用意に書き換える

------------------------------------------------------------------------

## 17. Beauty Archive固有の開発方針

Beauty Archive の現在の基本構成は以下とする。

``` text
Beauty Archive
├─ SwiftUI
├─ SwiftData
├─ CloudKit / iCloud
├─ UserNotifications
├─ PhotosPicker
├─ Google OAuth
└─ Google Calendar API
```

### 基本原則

-   iOS専用アプリとして開発する
-   Android / Web対応は現時点では対象外
-   Beauty Archive独自アカウントは初期リリースでは実装しない
-   SwiftDataをローカルデータの基本とする
-   CloudKit / iCloudで同じApple Accountの端末間同期を行う
-   Google OAuthはBeauty ArchiveログインではなくGoogle
    Calendar利用のための認可とする
-   Google Calendar未連携でもアプリの基本機能を利用可能にする
-   写真を重要なコンテンツとして扱う
-   UIはAppleらしいNative iOSデザインを優先する
-   シンプルで洗練されたメンズライクなデザインを基本とする
-   Liquid Glassは主に操作UI・Navigation・Tab Bar・Floating
    Control等に利用し、コンテンツの可読性を優先する
-   Apple純正APIが利用可能な場合は独自再現より純正実装を優先する

------------------------------------------------------------------------

## 18. 最終判断基準

Codex が各作業を「完了」と判断できるのは、以下の状態になったときのみ。

``` text
Issue作成（ユーザー指示による例外を除く）
    ✓
作業ブランチ作成
    ✓
実装
    ✓
Self Review
    ✓
アプリコード変更時はBuild成功／文書のみは差分確認
    ✓
必要な検証を実施・未実施項目を記録
    ✓
PR作成
    ✓
CI成功
    ✓
mainへマージ
    ✓
Issueクローズ
    ✓
不要ブランチ削除
```

このサイクルを Beauty Archive の標準開発フローとする。
